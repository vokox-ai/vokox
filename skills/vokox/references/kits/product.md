# Kit: product hero (image, then turn clip)

Image (`seedream-4.5` or `nano-banana-2`, refs = `["file:./product.png"]`):
```
{product name} exactly as in the reference: identical shape, label, colours and proportions.
Placed {surface: "on a matte black stone slab" | "on a sunlit oak table" | "floating on soft
white studio sweep"}. {light: "soft top light with a thin cool rim" | "warm window light from the
left"}. {angle: "three-quarter from slightly above" | "straight-on hero"}. 85mm lens, shallow
focus, {props: "two coffee beans and a folded linen napkin" | none}. Clean, premium, no text.
```

Turn clip (`auto:video.hq`, refs = `["@hero"]`):
```
Slow 90-degree orbit around the product, camera at eye level, speed constant; light glides across
the label; background and surface stay still; nothing else moves; ends centred and sharp.
```

Variants: change only `{surface}` and `{light}`; keep the first sentence verbatim so the product
stays identical across the series.
